
import { GuardShiftRequests } from "@/components/GuardShiftRequests";

const Requests = () => {
  return (
    <div className="min-h-screen bg-gradient-to-br from-vibrant-purple/10 to-sky-blue/10">
      <GuardShiftRequests />
    </div>
  );
};

export default Requests;
